#/bin/bash

SCRIPT_DIR="$(cd "$(dirname "$([ `readlink "$0"` ] && echo "`readlink "$0"`" || echo "$0")")"; pwd -P)"

cd "$SCRIPT_DIR"
# git submodule update --init --recursive
docker run -i -t --entrypoint /bin/bash "${1:-cm/core}"
