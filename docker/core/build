#/bin/bash

SCRIPT_DIR="$(cd "$(dirname "$([ `readlink "$0"` ] && echo "`readlink "$0"`" || echo "$0")")"; pwd -P)"

cd "$SCRIPT_DIR"
git submodule update --init --recursive
tar -czh . | docker build -t cm/core -